import SwiftUI

struct AppTheme {
    static let accent = Color(hex: "4A7C59")
    static let accentLight = Color(hex: "6B9E7A")
    static let accentDark = Color(hex: "355A40")
    static let background = Color(hex: "F7F5F0")
    static let cardBackground = Color.white
    static let secondaryText = Color(hex: "8A8A8A")
    static let warmGray = Color(hex: "C4BAA2")
    static let destructive = Color(hex: "C0392B")

    static let cornerRadius: CGFloat = 14
    static let cardCornerRadius: CGFloat = 16
    static let smallCornerRadius: CGFloat = 8
    static let horizontalPadding: CGFloat = 16
    static let cardShadow: Color = .black.opacity(0.06)
}
